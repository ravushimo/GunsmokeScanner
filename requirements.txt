pillow==12.2.0
PyAutoGUI==0.9.54
keyboard==0.13.5
opencv-python-headless==4.13.0.92
easyocr==1.7.2
pandas==3.0.2
numpy==2.4.4
colorama==0.4.6
requests==2.33.1
cryptography==47.0.0
PySide6-Essentials==6.11.1
pyinstaller==6.20.0
# Default PyPI wheels are CPU-only. After pip install, run:
#   python scripts/ensure_torch.py
# (or setup.bat option 1) to swap in CUDA torch when an NVIDIA GPU is detected.
# ensure_torch tries cu128 -> cu126 -> cu124, then falls back to CPU if CUDA fails.
torch==2.11.0
torchvision==0.26.0
